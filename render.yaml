services:
  - type: web
    name: full-swing-to-swingsync
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.x
      - key: VITE_GA_MEASUREMENT_ID
        sync: false
